import { motion } from "motion/react";
import { Reveal, SectionHeading } from "@/components/Section";

export function Gallery() {
  return (
    <section id="gallery" className="section-pad relative">
      {/* Gallery section is intentionally empty */}
    </section>
  );
}
